{
  "name": "@clawhouse/near-wallet",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun run src/cli.ts generate",
    "inspect": "bun run src/cli.ts inspect",
    "read-public": "bun run src/cli.ts read-public",
    "sign-request": "bun run src/cli.ts sign-request",
    "sign-agent-request": "bun run src/cli.ts sign-agent-request",
    "verify-request": "bun run src/cli.ts verify-request",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@near-js/crypto": "^2.5.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "typescript": "^5.9.3"
  }
}
